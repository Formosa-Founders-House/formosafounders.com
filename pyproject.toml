[project]
name = "app"
version = "0.1.0"
dependencies = [ "flask==3.1.3", "authlib>=1.6, <2", "gunicorn==26.0.0", "psycopg[binary]>=3.2, <4", "pyschlage==2026.7.0", "requests>=2.32, <3" ]
requires-python = "~=3.14.0"
